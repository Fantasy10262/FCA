# FCA 迁移云服务器 / 学生机 · 选型对比（2026-08）

> 目标：后期把 FCA（Flask + SQLite/Postgres 判题 OJ）从 Railway 迁到自有云服务器，长期比 Railway Hobby $5/月更省、且数据完全自控。
> 技术前提：判题在 Docker 容器内调 gcc/g++；`db.py` 双引擎已支持 `DATABASE_URL`，仓库已有 `Dockerfile`（含 gcc/g++ + waitress + 导入时 init_db）。**迁移基本是 clone + docker compose up，零代码改动。**

---

## 一、FCA 对服务器的硬需求
| 需求 | 说明 | 是否满足 |
|------|------|----------|
| Docker 支持 | 判题进程跑在容器里，宿主机只需能装 Docker | ✅ 所有主流 Linux VPS 都支持 |
| 内存 | 应用单独跑（DB 用 Supabase 免费）→ 2G 够；若 Postgres 也塞进同机 docker-compose → 至少 4G | ⚠️ 按方案选配置 |
| 常驻 | 班级 OJ 要 7×24 在线，不能像 Railway 免费档休眠 | ✅ 真实 VPS 均常驻 |
| 带宽 | 班级量级（几十~几百人）流量很小，3–5M 足够 | ✅ 都满足 |
| 地域 | 面向国内学生，国内节点延迟低 | ✅ 腾讯/阿里/雨云均有国内节点 |
| 备案 | 用平台 IP/临时域名访问**无需备案**；绑自定义域名（国内节点）需 ICP 备案（约 1–2 周） | ⚠️ 视是否绑域名 |

---

## 二、三家横向对比（2026 年价，均为新客/学生口径，随活动浮动）

| 厂商 | 推荐配置 | 到手价 | 续费 | 带宽/流量 | 一键 Docker | 适合 FCA？ |
|------|----------|--------|------|-----------|-------------|------------|
| **阿里云 轻量** | 2核2G / 40G / 200M | 秒杀 **38元/年**（每日10/15点），常规 68元/年 | 79元/年 | 200M峰值 / **不限流量** | ✅ 镜像含 Docker | ⭐⭐⭐ 最稳+便宜，配 Supabase 首选 |
| **腾讯云 轻量（校园）** | 2核2G / 4M / 300G / 40G | **25.92元/3月起**（≈104元/年） | 看当年活动 | 4M / 300G月流量 | ✅ 含宝塔面板 | ⭐⭐⭐ 校园认证最便宜 |
| **腾讯云 轻量（新客秒杀）** | 4核4G / 3M / 300G | **38元/年**（续费99元/年） | 99元/年 | 3M / 300G | ✅ | 配置翻倍且更便宜 |
| **雨云（国内）** | 2核4G / 8M / 40G | **~35元/月**（学生认证更低，年付7折更省） | 同价 | **不限流量** | ✅ | 价格低但小厂，稳定性需权衡 |
| **雨云（香港）** | 2核2G / 500M | **19.6元/月** | 同价 | 大带宽 / 限流量 | ✅ | 免备案、带宽大 |

> 注：雨云为新注册用户送首月5折券，可叠加年付7折；支持按小时计费、7天无理由退款、1元试用1天。阿里云/腾讯云轻量全系标配高带宽+不限流量（阿里云 200M），且有一键应用镜像，运维门槛最低。

---

## 三、三种落地路线（按「是否自托管数据库」划分）

### 路线 A：VPS 只跑应用 + 数据库继续用 Supabase（最省心，推荐起步）
- **配置**：任意 2核2G 轻量（阿里云38/年、腾讯云校园104/年、雨云2核2G~25元/月）。
- **做法**：VPS 只起 Flask 容器，`DATABASE_URL` 仍指向现有 Supabase 免费 Postgres → **零迁移、零数据搬运**。
- **优点**：本机只吃应用内存，2G 足够；Supabase 负责持久化与备份；最便宜。
- **缺点**：数据库仍依赖 Supabase（但其免费档对班级 OJ 完全够）。

### 路线 B：VPS 整机自托管（应用 + Postgres 都在自己服务器，彻底不依赖外部）
- **配置**：至少 **2核4G**（阿里云轻量 2核4G ≈ 199元/年；雨云 2核4G ≈ 35元/月不限流量）。
- **做法**：`docker-compose` 同时起 `app` + `postgres` 两个服务，把 `DATABASE_URL` 指向容器内的 Postgres。
- **优点**：数据 100% 自主管控，不依赖任何第三方；雨云不限流量更爽。
- **缺点**：需自己管数据库备份；2G 内存不够（Postgres+Flask+判题会挤）。

### 路线 C：想免备案 / 直接绑域名即用
- **配置**：雨云香港 2核2G（19.6元/月，500M 大带宽）或阿里云轻量香港（~300元/年）。
- **优点**：香港节点**免 ICP 备案**，开机即用自定义域名；带宽大。
- **缺点**：价格略高；国内访问延迟比大陆节点高一点。

---

## 四、结论与建议
1. **性价比+稳定性首选**：阿里云或腾讯云**轻量 2核2G**（年付 38–104 元）+ **Supabase 免费 Postgres（路线 A）**。Docker 一键镜像、7×24 常驻、OJ 全功能可跑，长期成本最低且最稳。
2. **想彻底自托管、不依赖 Supabase**：选 **2核4G**（阿里云199/年 或 雨云~35元/月），走路线 B 的 docker-compose（app+postgres 一体）。
3. **怕备案/要免备案**：雨云香港 2核2G（19.6元/月）或阿里云轻量香港。
4. **学生认证**：腾讯云「云+校园」、阿里云「云翼计划」均需学信网/教育认证，25岁以下大概率免学生证；雨云也有学生认证额外折扣。
5. **迁移成本极低**：仓库已有 `Dockerfile` + 双引擎 `db.py`。届时 VPS 装好 Docker → `git clone` → `docker compose up` 即可，基本零代码改动。

---

## 五、待办（决策后我可立即帮你准备）
- [ ] 写 `docker-compose.yml`（路线 A：仅 app，连 Supabase；或路线 B：app + postgres）。
- [ ] 加 Nginx 反代 + 自动 HTTPS（Let's Encrypt / 1Panel 一键）模板。
- [ ] 写一份「VPS 部署步骤」文档（装 Docker → clone → compose up → 填 DATABASE_URL → 开机自启）。
- [ ] 当前 Railway 站点继续跑，迁移时切 DNS/域名即可，零停机。
