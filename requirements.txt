Flask>=3.0
Flask-WTF>=1.2   # CSRF 防护：全站表单统一校验 csrf_token
requests>=2.25   # 仅 pta_import.py 抓取脚本需要
waitress>=2.1    # 生产 WSGI 服务器（Windows / 跨平台，单进程多线程，对 SQLite 友好）
psycopg2-binary>=2.9  # 接入 Supabase 免费 Postgres（仅设了 DATABASE_URL 时才 import，本地 SQLite 无需）
# gunicorn>=21.0  # Linux/macOS 生产服务器备选
