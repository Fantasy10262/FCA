# Fantasy Coding Arena（FCA）部署上线指南

本指南覆盖三种上线方式，从「本机局域网内访问」到「公网生产环境」。请按自己的需求选择。

---

## 0. 部署前准备（重要）

本项目是 **Flask + SQLite + 本地子进程判题（gcc / g++ / python）**，因此：

1. **判题依赖编译器**：服务器上必须安装对应语言运行时——
   - C/C++：安装 `gcc` / `g++`（MinGW-w64 on Windows，或 `build-essential` on Linux）。
   - Python：安装与开发机相同的 Python 版本。
   判题时会在服务器本地调用编译器编译并运行学生代码，**没有编译器就无法判题**。
2. **数据库是单文件 SQLite**：位于 `data/oj.db`，零配置、自动随项目走，备份只需复制该文件。
3. **密钥**：生产环境务必设置环境变量 `OJ_SECRET`（否则用默认弱密钥，会话可被伪造）。
4. **当前开发服务器仅供开发**：`app.run(...)` 是 Flask 自带开发服务器，公网请用生产 WSGI 服务器。

---

## 1. 方式一：本机 / 局域网快速访问（最快，适合课堂演示）

代码已经 `app.run(host="0.0.0.0", port=5000)`，所以本机之外的同网段设备也能直接访问。

```bash
# 用带 Flask 的虚拟环境（系统/托管 Python 没装 flask）
C:\Users\lenovo\.workbuddy\binaries\python\envs\default\Scripts\python.exe app.py
```

- 本机打开：http://localhost:5000
- 同局域网其他同学打开：http://<你的内网IP>:5000（如 http://192.168.1.10:5000）
- 查内网 IP：`ipconfig`（Windows）/`ip addr`（Linux）看 IPv4。

> 注意：开发服务器性能有限、且关闭终端即停。仅适合临时演示，不建议长期对外。

---

## 2. 方式二：生产级运行（WSGI 服务器）

### 2.1 安装生产服务器

- **Windows 首选 `waitress`**（纯 Python，跨平台，单进程多线程，对 SQLite 最友好）：
  ```bash
  pip install waitress
  ```
- **Linux / macOS 首选 `gunicorn`**：
  ```bash
  pip install gunicorn
  ```

### 2.2 启动命令

waitress（推荐，避免多进程并发写 SQLite 锁表）：
```bash
# 在 venv 中
waitress-serve --listen=0.0.0.0:5000 --threads=8 app:app
```

gunicorn（Linux，建议单 worker + 多线程，减轻 SQLite 锁）：
```bash
gunicorn -w 1 --threads 8 -b 0.0.0.0:5000 app:app
# 若机器核数多且只读为主，可 -w 4，但判题写库可能偶发 locked，谨慎
```

### 2.3 启用 WAL（减少锁等待，可选）
在 `app.py` 的 `get_db()` 里连接后执行一次：
```python
db.execute("PRAGMA journal_mode=WAL")
```
（当前代码已开启外键；加这一行即可。）

### 2.4 设为后台常驻

- **Windows**：用 `nssm` 把上面的命令注册成系统服务；或写个 `.bat` 用任务计划程序开机自启。
- **Linux**：用 `systemd` 托管（见下）。

`/etc/systemd/system/fca.service` 示例：
```ini
[Unit]
Description=FCA Online Judge
After=network.target

[Service]
WorkingDirectory=/opt/fca
ExecStart=/opt/fca/venv/bin/waitress-serve --listen=0.0.0.0:5000 --threads=8 app:app
Environment=OJ_SECRET=换成一段随机长字符串
Restart=always
User=www-data

[Install]
WantedBy=multi-user.target
```
```bash
sudo systemctl daemon-reload && sudo systemctl enable --now fca
```

---

## 3. 方式三：放到公网（云服务器 / PaaS）

### 3.1 云服务器（阿里云 / 腾讯云 / AWS / 华为云 ECS）
1. 买一台轻量应用服务器（1 核 2G 足够 40 人）。
2. 装 Python 3.11+、gcc/g++、并 `pip install flask waitress`（或 gunicorn）。
3. 把整个项目目录上传（含 `data/oj.db`、`static/`、`templates/`）。
4. 用 2.4 的 systemd 跑起来，开放安全组 **5000** 端口（或 80/443，见 4）。
5. 用 `http://<公网IP>:5000` 访问。**建议套反向代理 + HTTPS（下一步）**。

### 3.2 反向代理 + HTTPS（强烈推荐）

**Nginx（Linux）** 把 80/443 转发到 5000，并托管静态文件提速：

```nginx
server {
    listen 80; server_name fca.yourdomain.com;
    location /static { alias /opt/fca/static; expires 30d; }
    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

**HTTPS**：用 `certbot --nginx` 一键申请 Let's Encrypt 证书，或换用 **Caddy**（自动 HTTPS，配置更短）：
```caddy
fca.yourdomain.com {
    reverse_proxy 127.0.0.1:5000
    encode gzip
}
```

### 3.3 免运维 PaaS（不想管服务器）
- **Railway / Render / Fly.io**：直接连 Git 仓库部署。`requirements.txt` 已有 `Flask`。
  - 在 `requirements.txt` 追加 `waitress`（或 `gunicorn`）。
  - 启动命令设为 `waitress-serve --port=$PORT --threads=8 app:app`（Render/Railway 注入 `$PORT`）。
  - 注意：这些平台的文件系统** ephemeral（临时）**，SQLite 数据重启会丢——需要挂持久卷（Volume）或改用 PostgreSQL（改动较大）。适合演示，慎用于正式考试。
- **PythonAnywhere**（最省心，原生支持 Flask + 持久文件）：
  - 上传项目，Manual configuration 选 Python 3.11，WSGI 文件指向 `app:app`。
  - 免费版仅支持 `*.pythonanywhere.com` 域名且限带宽，适合小范围。

---

## 4. 生产环境检查清单

- [ ] 设置 `OJ_SECRET` 环境变量（随机长字符串）。
- [ ] 用 `waitress` / `gunicorn` 代替 `app.run` 的开发服务器。
- [ ] 确认服务器已装 `gcc`、`g++`、`python`，且 `judge.detect_languages()` 在日志输出可用语言。
- [ ] 数据库 `data/oj.db` 已备份并随项目迁移（PaaS 记得挂持久卷）。
- [ ] 用 Nginx/Caddy 反向代理 + HTTPS（公网必做）。
- [ ] 用 systemd / nssm 设为开机自启、崩溃自动重启。
- [ ] 防火墙只放行 80/443（或 5000），不用 root 直接跑。

---

## 5. 迁移 / 备份数据库

```bash
# 备份
cp data/oj.db data/oj.db.bak.$(date +%F)

# 迁移到新机器：把整个项目目录（含 data/）整体拷贝即可，无需另建库
```

---

## 6. 目录结构与常用命令速查

```
app.py              主程序（路由 + 判题调度），生产用 app:app 入口
judge.py            多语言判题引擎（gcc/g++/python 子进程）
static/style.css     前端样式（本版为深色极光玻璃风格）
templates/           Jinja2 页面模板
data/oj.db           SQLite 数据库（题目/学生/提交/题目集）
```

```bash
# 开发预览
python app.py
# 生产
waitress-serve --listen=0.0.0.0:5000 --threads=8 app:app
```
